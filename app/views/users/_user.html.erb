<li>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "verwijderen", user, method: :delete,
                                  data: { confirm: "Zeker weten?" } %>
  <% end %>
</li>